const state={page:'Overview',role:'Customer',bookings:[{id:'TW-10482',route:'Kampala → Mukono',status:'In Transit',customer:'Amina Traders',amount:15000,partner:'Kira Central DropPoint',category:'Business stock',weight:4},{id:'TW-10481',route:'Wakiso → Kampala',status:'Delivered',customer:'Brian Okello',amount:12000,partner:'Kampala Hub',category:'Documents',weight:1}],storage:[{id:'ST-2004',place:'Kira Storage Hub',status:'In Storage',days:7,amount:17000,goods:'Business stock'}],journeys:[],partners:[{name:'Kira Central DropPoint',type:'DropPoint',status:'Approved'},{name:'Mukono Micro Warehouse',type:'Storage',status:'Under Review'},{name:'David K.',type:'Driver',status:'Approved'}],lastTrack:'TW-10482'};
const money=n=>'UGX '+Number(n||0).toLocaleString();
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function icon(p){return ({Overview:'▦','Book Delivery':'＋',Storage:'▤','Track Parcel':'⌕',Journeys:'↗',Partners:'♙',Admin:'⚙'})[p]||'•'}
function go(p){state.page=p;render()}
function render(){document.querySelector('#app').innerHTML=`<div class="layout"><aside class="sidebar"><div class="brand"><span class="brand-mark">↗</span> TwalaLink</div><div class="tag">Move Goods. Connect Routes.</div><div class="role-switch"><label>View as</label><select onchange="state.role=this.value;render()"><option ${state.role==='Customer'?'selected':''}>Customer</option><option ${state.role==='Driver'?'selected':''}>Driver</option><option ${state.role==='Admin'?'selected':''}>Admin</option></select></div><nav class="nav">${['Overview','Book Delivery','Storage','Track Parcel','Journeys','Partners','Admin'].map(p=>`<button class="${state.page===p?'active':''}" onclick="go('${p}')"><span>${icon(p)}</span>${p}</button>`).join('')}</nav><div class="side-note"><b>Prototype mode</b><br>Payments, verification and payouts are simulated for testing.</div></aside><main class="main"><header class="top"><div><div class="eyebrow">TWALALINK LOGISTICS</div><div class="title">${state.page}</div><div class="muted small">Uganda pilot · Kampala, Wakiso & Mukono</div></div><div class="top-right"><span class="pill success">● Demo online</span><div class="avatar">SN</div></div></header>${page()}</main></div>`}
function page(){switch(state.page){case'Book Delivery':return delivery();case'Storage':return storage();case'Track Parcel':return tracking();case'Journeys':return journeys();case'Partners':return partners();case'Admin':return admin();default:return overview()}}
function stat(label,value,sub){return `<div class="card stat-card"><div class="stat-label">${label}</div><div class="stat-value">${value}</div><div class="small muted">${sub}</div></div>`}
function overview(){return `<section class="hero"><div><span class="pill success">Connected logistics network</span><h1>Move goods with trusted routes.</h1><p class="muted">Send parcels, reserve storage, connect with delivery partners and follow each handover in one place.</p><div class="actions"><button class="btn" onclick="go('Book Delivery')">＋ Book a delivery</button><button class="btn secondary" onclick="go('Storage')">▤ Find storage</button></div></div><div class="route-preview"><div class="small muted">Featured route</div><h3>Kampala → Mukono</h3><div class="route-step"><span class="route-dot"></span><div><b>Kampala Hub</b><div class="small muted">Pickup confirmed</div></div></div><div class="route-connector"></div><div class="route-step"><span class="route-dot"></span><div><b>Kira Central DropPoint</b><div class="small muted">Approved handover partner</div></div></div><div class="route-connector"></div><div class="route-step"><span class="route-dot"></span><div><b>Mukono</b><div class="small muted">Expected today · 16:30</div></div></div></div></section><div class="grid stats">${stat('Active deliveries','12','+3 today')}${stat('Storage bookings','8','2 expiring soon')}${stat('Approved partners','24','4 under review')}${stat('Pending payouts',money(86000),'Simulated')}</div><section class="section"><div class="section-head"><div><h2>Quick actions</h2><p class="muted small">Start with the service you need.</p></div></div><div class="grid cards four"><button class="card action-card" onclick="go('Book Delivery')"><span class="big-icon">📦</span><b>Send a parcel</b><span class="small muted">Get a price and booking number.</span></button><button class="card action-card" onclick="go('Storage')"><span class="big-icon">🏬</span><b>Find storage</b><span class="small muted">Reserve space for your goods.</span></button><button class="card action-card" onclick="go('Track Parcel')"><span class="big-icon">📍</span><b>Track parcel</b><span class="small muted">Check status and handovers.</span></button><button class="card action-card" onclick="go('Journeys')"><span class="big-icon">🚚</span><b>Declare route</b><span class="small muted">Offer available cargo space.</span></button></div></section><section class="section"><div class="section-head"><div><h2>Recent deliveries</h2><p class="muted small">Your latest demo bookings.</p></div><button class="btn ghost" onclick="go('Track Parcel')">Track a parcel →</button></div><div class="card table-wrap">${bookingTable()}</div></section><section class="grid cards three"><div class="card feature-card"><span class="eyebrow">SAFE HANDOVERS</span><h3>Approved partners</h3><p class="muted small">Parcels move through registered and approved DropPoints.</p></div><div class="card feature-card"><span class="eyebrow">TRANSPARENT PAYMENTS</span><h3>Advance payment</h3><p class="muted small">See pricing before booking. Driver earnings stay pending until confirmation.</p></div><div class="card feature-card"><span class="eyebrow">SMART ROUTES</span><h3>Route matching</h3><p class="muted small">Matching checks capacity, stops, timing and suitable handover locations.</p></div></section>`}
function bookingTable(){return `<table class="table"><thead><tr><th>Tracking</th><th>Route</th><th>Status</th><th>Amount</th><th>Partner</th></tr></thead><tbody>${state.bookings.map(b=>`<tr><td><b>${esc(b.id)}</b><div class="small muted">${esc(b.category||'Parcel')}</div></td><td>${esc(b.route)}</td><td><span class="pill ${b.status==='Delivered'?'success':'warn'}">${esc(b.status)}</span></td><td>${money(b.amount)}</td><td>${esc(b.partner)}</td></tr>`).join('')}</tbody></table>`}
function delivery(){return `<section class="card form-card"><div class="section-head"><div><span class="eyebrow">CUSTOMER SERVICE</span><h2>Book a delivery</h2><p class="muted small">Enter parcel details to calculate a demo price and create a tracking number.</p></div><span class="pill">UGX</span></div><form onsubmit="createDelivery(event)"><div class="form-grid"><div class="field"><label>Pickup location</label><select name="from"><option>Kampala</option><option>Wakiso</option><option>Kira</option><option>Entebbe</option><option>Nansana</option></select></div><div class="field"><label>Destination</label><select name="to"><option>Mukono</option><option>Kampala</option><option>Wakiso</option><option>Masaka</option><option>Entebbe</option></select></div><div class="field"><label>Parcel category</label><select name="category"><option>Documents</option><option>Clothing</option><option>Food products</option><option>Electronics</option><option>Business stock</option></select></div><div class="field"><label>Weight (kg)</label><input name="weight" type="number" min="0.1" step="0.1" value="2" required></div><div class="field"><label>Length (cm)</label><input name="length" type="number" min="1" value="30" required></div><div class="field"><label>Width (cm)</label><input name="width" type="number" min="1" value="20" required></div><div class="field"><label>Height (cm)</label><input name="height" type="number" min="1" value="15" required></div><div class="field"><label>Delivery option</label><select name="speed"><option>Standard</option><option>Priority</option></select></div><div class="field"><label>Receiver name</label><input name="receiver" placeholder="Receiver name" required></div><div class="field full"><label>Instructions</label><textarea name="notes" placeholder="DropPoint preference, handling notes, etc."></textarea></div></div><div class="notice" id="estimate">Calculate a price before confirming your demo booking.</div><div class="actions"><button type="button" class="btn secondary" onclick="estimateDelivery()">Calculate price</button><button class="btn">Pay & create booking</button></div></form></section>`}
function calc(f){const w=+f.weight.value||1;const volume=(+f.length.value||1)*(+f.width.value||1)*(+f.height.value||1);return Math.round(8000+w*1200+Math.ceil(volume/5000)*1000+(f.speed.value==='Priority'?4000:0))}
function estimateDelivery(){const f=document.querySelector('form');document.querySelector('#estimate').innerHTML=`<b>Estimated total: ${money(calc(f))}</b><br><span class="small">Includes route, weight, bulkiness and handling estimate.</span>`}
function createDelivery(e){e.preventDefault();const f=e.target,price=calc(f),id='TW-'+(10500+state.bookings.length+Math.floor(Math.random()*80));const b={id,route:`${f.from.value} → ${f.to.value}`,status:'Payment Confirmed',customer:'Demo Customer',amount:price,partner:'Pending assignment',category:f.category.value,weight:+f.weight.value,receiver:f.receiver.value};state.bookings.unshift(b);state.lastTrack=id;alert(`Demo payment successful. ${id} created for ${money(price)}.`);go('Track Parcel')}
function storage(){return `<div class="grid cards three">${stat('Available demo space','68%','Across sample storage partners')}${stat('Active bookings',String(state.storage.length),'Demo records')}${stat('Expiring soon','2','Extension requires payment')}</div><section class="card form-card"><div class="section-head"><div><span class="eyebrow">STORAGE NETWORK</span><h2>Reserve storage</h2><p class="muted small">Book temporary holding or business inventory space.</p></div><span class="pill">Advance payment</span></div><form onsubmit="createStorage(event)"><div class="form-grid"><div class="field"><label>Storage location</label><select name="place"><option>Kira Storage Hub</option><option>Mukono Micro Warehouse</option><option>Kampala Business Store</option></select></div><div class="field"><label>Duration (days)</label><input name="days" type="number" value="7" min="1" required></div><div class="field"><label>Packages</label><input name="packages" type="number" value="1" min="1" required></div><div class="field"><label>Goods description</label><input name="goods" value="Business stock" required></div><div class="field full"><label>Special requirements</label><textarea name="notes" placeholder="Temperature, handling or collection notes"></textarea></div></div><div class="notice"><b>Demo fee:</b> days × UGX 2,000 + UGX 3,000 handling fee.</div><div class="actions"><button class="btn">Pay & book storage</button></div></form></section><section class="section"><div class="section-head"><h2>Storage bookings</h2></div><div class="card table-wrap"><table class="table"><thead><tr><th>Booking</th><th>Location</th><th>Status</th><th>Duration</th><th>Amount</th></tr></thead><tbody>${state.storage.map(s=>`<tr><td><b>${esc(s.id)}</b><div class="small muted">${esc(s.goods||'Goods')}</div></td><td>${esc(s.place)}</td><td><span class="pill success">${esc(s.status)}</span></td><td>${s.days} days</td><td>${money(s.amount)}</td></tr>`).join('')}</tbody></table></div></section>`}
function createStorage(e){e.preventDefault();const f=e.target,days=+f.days.value,amount=days*2000+3000,id='ST-'+(2005+state.storage.length+Math.floor(Math.random()*50));state.storage.unshift({id,place:f.place.value,status:'Payment Confirmed',days,amount,goods:f.goods.value});alert(`Demo payment successful. ${id} created for ${money(amount)}.`);go('Storage')}
function tracking(){return `<section class="card form-card"><div class="section-head"><div><span class="eyebrow">PARCEL VISIBILITY</span><h2>Track your parcel</h2><p class="muted small">Use a tracking number from your booking.</p></div><span class="pill">QR-ready demo</span></div><div class="track-search"><input id="tracking-input" placeholder="e.g. TW-10482" value="${esc(state.lastTrack)}"><button class="btn" onclick="track()">Search tracking</button></div><div id="tracking-result">${trackingResult(state.bookings.find(b=>b.id===state.lastTrack)||state.bookings[0])}</div></section>`}
function trackingResult(b){if(!b)return '<div class="notice">No parcel found. Create a booking first.</div>';const steps=['Booking Created','Payment Confirmed','Assigned to Delivery Partner','Journey Started','At Route Checkpoint','Arrived at Destination Partner','Ready for Collection','Delivered'];const current=b.status==='Delivered'?7:b.status==='In Transit'?4:1;return `<div class="track-summary"><div><span class="eyebrow">TRACKING NUMBER</span><h2>${esc(b.id)}</h2><p class="muted">${esc(b.route)}</p></div><span class="pill ${b.status==='Delivered'?'success':'warn'}">${esc(b.status)}</span></div><div class="grid cards three"><div class="card mini"><span class="muted small">Amount</span><b>${money(b.amount)}</b></div><div class="card mini"><span class="muted small">Partner</span><b>${esc(b.partner)}</b></div><div class="card mini"><span class="muted small">Category</span><b>${esc(b.category||'Parcel')}</b></div></div><div class="timeline">${steps.map((s,i)=>`<div class="event ${i<=current?'done':''}"><span class="event-dot">${i<=current?'✓':''}</span><div><b>${s}</b><div class="muted small">${i<=current?'Confirmed in demo timeline':'Pending confirmation'}</div></div></div>`).join('')}</div><div class="notice">In a live version, this timeline will update through QR scans, authorized partner confirmations and delivery evidence.</div>`}
function track(){const id=document.querySelector('#tracking-input').value.trim();state.lastTrack=id;document.querySelector('#tracking-result').innerHTML=trackingResult(state.bookings.find(b=>b.id===id))}
function journeys(){return `<section class="card form-card"><div class="section-head"><div><span class="eyebrow">DELIVERY PARTNER</span><h2>Declare my route</h2><p class="muted small">Offer available cargo space for suitable parcels.</p></div><span class="pill">Driver view</span></div><form onsubmit="declareJourney(event)"><div class="form-grid"><div class="field"><label>Origin</label><select name="from"><option>Kampala</option><option>Wakiso</option><option>Masaka</option></select></div><div class="field"><label>Destination</label><select name="to"><option>Mukono</option><option>Entebbe</option><option>Masaka</option></select></div><div class="field"><label>Departure</label><input name="departure" type="datetime-local" required></div><div class="field"><label>Available capacity (kg)</label><input name="capacity" type="number" value="30" min="1" required></div><div class="field full"><label>Planned stops</label><input name="stops" placeholder="Kira, Seeta, Mukono"></div><div class="field full"><label>Restrictions</label><textarea name="restrictions" placeholder="No fragile goods, liquids or oversized items"></textarea></div></div><div class="actions"><button class="btn">Save journey</button></div></form></section><section class="section"><div class="section-head"><h2>Delivery opportunities</h2><span class="pill success">Demo matches</span></div><div class="grid cards two"><div class="card opportunity"><div class="row-between"><span class="pill">Route match</span><b>UGX 10,000</b></div><h3>Kampala → Mukono</h3><p class="muted small">3 parcels · 18 kg · Departure 2:30 PM</p><p class="small">Approved receiving partner required before confirmation.</p><button class="btn" onclick="alert('Demo opportunity selected. Admin/partner checks still required.')">View opportunity</button></div><div class="card opportunity"><div class="row-between"><span class="pill">Route match</span><b>UGX 14,000</b></div><h3>Kampala → Entebbe</h3><p class="muted small">2 parcels · 10 kg · Departure 4:00 PM</p><p class="small">Capacity and parcel restrictions are checked.</p><button class="btn" onclick="alert('Demo opportunity selected. Admin/partner checks still required.')">View opportunity</button></div></div></section>`}
function declareJourney(e){e.preventDefault();const f=e.target;state.journeys.unshift({from:f.from.value,to:f.to.value,departure:f.departure.value,capacity:f.capacity.value,stops:f.stops.value});alert('Demo journey saved. Matching will require an approved DropPoint before confirmation.');go('Journeys')}
function partners(){return `<section class="section-head"><div><h2>Partner directory</h2><p class="muted small">Approved DropPoints, storage partners and delivery partners.</p></div><button class="btn" onclick="alert('Demo partner registration form: business details, location, documents and capacity.')">＋ Register partner</button></section><div class="grid cards three">${state.partners.map(p=>`<div class="card partner-card"><div class="row-between"><span class="big-icon">${p.type==='Driver'?'🚚':p.type==='Storage'?'🏬':'📍'}</span><span class="pill ${p.status==='Approved'?'success':'warn'}">${p.status}</span></div><h3>${esc(p.name)}</h3><p class="muted small">${esc(p.type)}</p><button class="btn ghost" onclick="alert('Demo review: ${esc(p.name)}')">Review partner</button></div>`).join('')}</div>`}
function admin(){return `<div class="grid stats">${stat('Pending verification','6','Drivers and vehicles')}${stat('Open disputes','2','Needs review')}${stat('Today revenue',money(126000),'Demo totals')}${stat('Storage occupancy','68%','Sample network')}</div><section class="section grid cards three"><div class="card"><span class="eyebrow">VERIFICATION</span><h3>Partner approvals</h3><p class="muted small">Review identity, vehicle, insurance and business documents.</p><button class="btn secondary" onclick="alert('Demo approval queue opened.')">Open queue</button></div><div class="card"><span class="eyebrow">PRICING</span><h3>Pricing controls</h3><p class="muted small">Configure route, weight, bulkiness, storage and commission rules.</p><button class="btn secondary" onclick="alert('Demo pricing controls opened.')">Manage pricing</button></div><div class="card"><span class="eyebrow">SETTLEMENTS</span><h3>Payout review</h3><p class="muted small">Release driver earnings only after approved delivery confirmation.</p><button class="btn secondary" onclick="alert('Demo payout review opened.')">Review payouts</button></div></section><section class="card"><h2>System safeguards</h2><ul class="muted small"><li>Only approved partners should receive parcels.</li><li>Payment is simulated until a provider is connected.</li><li>Driver earnings remain pending until delivery confirmation.</li><li>Private phone numbers are not displayed.</li><li>GPS should be limited to active deliveries with consent.</li></ul></section>`}
render();
